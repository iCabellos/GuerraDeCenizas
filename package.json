{
  "name": "guerra-de-cenizas",
  "version": "0.2.0",
  "private": true,
  "description": "4X multijugador por turnos, mobile-first, centrado en la diplomacia.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @gdc/web",
    "build": "npm run build -w @gdc/web",
    "test": "vitest run --root packages/core",
    "test:watch": "vitest --root packages/core",
    "test:security": "vitest run --root apps/web",
    "typecheck": "tsc --noEmit -p packages/core/tsconfig.json && npm run typecheck -w @gdc/web",
    "check:deps": "node tools/check-deps.mjs",
    "assets:build": "node tools/assets/build.mjs",
    "db:up": "node tools/pg/harness.mjs up",
    "db:reset": "node tools/pg/harness.mjs reset",
    "db:down": "node tools/pg/harness.mjs down",
    "db:psql": "node tools/pg/harness.mjs psql",
    "docs:pdf": "node tools/docs-pdf/build.mjs",
    "docs:check": "node tools/docs-pdf/check-links.mjs",
    "verify": "npm run typecheck && npm run check:deps && npm test && npm run test:security && npm run docs:check && npm run build"
  },
  "devDependencies": {
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^9.2.0",
    "playwright-core": "^1.56.1",
    "vitest": "^3.2.4"
  }
}
